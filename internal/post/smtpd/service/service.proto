syntax = "proto3";

// protoc --go_out=plugins=grpc:. *.proto

package service;

message Email {
    string From = 1;
    string To = 2;
    string Body = 3;
}
message Empty {
    bool s = 1;
}
service SmtpServer {
    rpc Enqueue(Email) returns (Empty) {}
}